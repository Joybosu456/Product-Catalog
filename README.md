# Product Catalog (Next.js + Redux Toolkit)

Small example app demonstrating product CRUD and a cart using Redux Toolkit.

Quick start

1. Install dependencies:

```powershell
cd d:\heloProject
npm install
```

2. Run dev server:

```powershell
npm run dev
```

Open http://localhost:3000
